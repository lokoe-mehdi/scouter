/vendor/
# Ancrés à la racine : sinon `storage/` (par ex.) matchait AUSSI le package
# crawler-go/internal/storage/ et son code partait du repo en silence.
/logs/
/crawls/
/storage/
composer.lock
.env
.claude
.phpunit.cache/
# Composer binary — le build Docker utilise l'image composer:latest, pas ce phar
composer.phar
.env.autosize
