{
  "name": "scouter-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Model Context Protocol server for Scouter — thin adapter over the public REST API (/api/v1).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2"
  }
}
