# PostgreSQL Configuration
POSTGRES_DB=scouter
POSTGRES_USER=scouter
POSTGRES_PASSWORD=130036d0250bb56919a11f8a00eb5dc8

# ClickHouse Configuration
CLICKHOUSE_DB=scouter
CLICKHOUSE_USER=scouter
CLICKHOUSE_PASSWORD=9fb5f2d12ec56ec7301bcb46f33880f9

# Clé de chiffrement maître (AES-256-GCM) — générée avec `openssl rand -hex 32`.
# NE PAS committer ce fichier (.env est dans .gitignore). À sauvegarder précieusement.
SCOUTER_ENCRYPTION_KEY=a3f879d65a07e29babfc0e09495ef30f65666996a899ec18759aa43742d58c94


# STORAGE_PATH is preset to /app/storage in docker-compose; override only off-Docker.
#STORAGE_PATH=./storage
S3_BUCKET=scouter
S3_REGION=
# S3_ENDPOINT: leave empty for AWS S3. For Cloudflare R2 / MinIO / Backblaze B2,
# set the endpoint URL (e.g. https://<account>.r2.cloudflarestorage.com).
S3_ENDPOINT=fsn1.your-objectstorage.com
S3_ACCESS_KEY_ID=KLT3J8YVUNJTP774YX27
S3_SECRET_ACCESS_KEY=w52tFjiTWM8KhZWCj56pYUE8Jxtes3v1F2PI0jUA
# S3_PREFIX: optional key namespace if the bucket is shared with other data.
S3_PREFIX=
# S3_USE_PATH_STYLE: set true for providers needing path-style addressing (MinIO).
S3_USE_PATH_STYLE=false
